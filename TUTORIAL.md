# 南邮羽毛球预约使用教程

> 本教程覆盖两种使用形态：
> - **绿色免安装版**（推荐）：`badminton-green.zip`，解压即用，无需安装 Python 等任何软件，双击 `.bat` 即可使用。→ 看第一部分
> - **源码版**：本仓库代码，需要自己安装 Python 环境，用命令行运行。→ 看第五部分

---

# 第一部分：绿色免安装版

## 1.1 前置条件

| 项目 | 要求 |
|---|---|
| 操作系统 | Windows 10 / 11（64 位） |
| 电脑微信 | 已安装，且登录的是绑定了学号的微信 |
| 网络 | 能访问 `wechat.njupt.edu.cn`（校园网/公网均可） |

绿色包内部已包含：嵌入式 Python 3.10、全部依赖库（requests / pyyaml / rich / plyer）、mitmproxy 抓包组件。**目标机器不需要安装任何东西。**

## 1.2 解压

把 `badminton-green.zip` 解压到**不含中文、不含空格**的路径：

- 推荐：`D:\badminton`
- 不推荐：`D:\下载\羽毛球`、`C:\Users\张三\Desktop\my tools`

解压后的目录结构：

```
badminton-green\
├── 0-首次配置.bat          首次使用向导（装证书、填学号、注册计划任务）
├── 1-刷新token.bat         每天抢票前刷新登录凭证
├── 2-抢票.bat              抢票（自动等到 12:00）
├── 配置向导-选场地.bat      换校区 / 换场次
├── 查看场次.bat             查看今天可约场地
├── 环境诊断.bat             出问题时自查
├── 卸载.bat                 一键卸载清理（删任务/证书/代理残留）
├── 使用说明.txt
├── app\                    程序与配置（config.yaml 在 app\config\）
└── python\                 内嵌 Python 环境（勿动）
```

**关于 Windows 安全提示**：网上下载的压缩包带有"网络来源标记"，首次双击 `.bat` 时可能弹出蓝色窗口 **"Windows 已保护你的电脑"**。这是系统对未知来源文件的常规提示（SmartScreen），点击 **更多信息 → 仍要运行** 即可，之后不再出现。

## 1.3 首次配置（只做一次）

双击 `0-首次配置.bat`，依次进行：

**第 1 步：准备证书组件**（自动，几秒钟）

```
[1/2] 准备证书组件（首次运行约需几秒钟）...
```

**第 2 步：填写学号**

```
[2/2] 运行配置向导...
=== 南邮羽毛球预约 配置向导 ===
学号>
```

输入你的学号，回车。

**第 3 步：安装抓包证书（会弹 Windows 安全警告）**

此时屏幕可能弹出一个 **安全警告对话框**（标题为"安全警告"），内容为"你即将从自称 mitmproxy 的机构安装证书"：

- **请点击【是】**。这是抓包组件的根证书，装在你当前 Windows 用户名下（无需管理员），作用是让微信小程序信任本机抓包。
- 点"否"或超时未点（约两分钟）会导致安装失败——重新双击 `0-首次配置.bat` 再来一次即可。
- 以后想卸载：双击 `卸载.bat` 会自动删除该证书（见第六部分）。

**第 4 步：是否注册每日计划任务**

```
是否注册每日计划任务（11:30 刷新 token + 11:55 抢票）？[y/N]
```

- 输入 `y` 回车（推荐）：注册两个每日任务——11:30 自动刷新登录、11:55 自动抢票，之后基本无需手动操作
- 输入 `n`：以后每天手动双击 bat

看到 `=== 完成！每日使用 ===` 即首配成功。

## 1.4 每日使用流程

### 第 1 步：刷新 token（每天抢票前做一次）

双击 `1-刷新token.bat`。窗口显示"系统代理已开启"后：

1. 打开**电脑微信** → 找到**南邮小程序** → 点进**场地预约**页面
2. 等 5~20 秒，窗口出现 `[+] 新 token 已捕获并保存` → 成功，脚本自动收尾退出

> **token 是什么**：微信登录状态的临时凭证，约 3 小时过期，所以每天抢票前要刷新一次。抓包原理：脚本临时把系统代理切到本机抓包端口，从小程序的请求中提取登录凭证，结束后**自动恢复系统代理原状**。

**一直抓不到时的排查顺序**：

1. 确认进入的是"场地预约"页面（只打开聊天窗口不产生流量）
2. 把电脑微信**完全退出**（右下角托盘图标右键 → 退出）再打开，重新进入场地页
3. 关闭 Fiddler 等其他抓包/代理工具后重试
4. 运行 `环境诊断.bat` 看哪一项 `[FAIL]`

### 第 2 步：抢票

双击 `2-抢票.bat`：

- **12 点前运行**：脚本自动等待至 12:00:00.000 准时发起预约（精确到毫秒）
- 预约成功显示成功信息；首选场次已满会**自动顺延**到其他已选场次
- 已注册计划任务的用户，每天 11:55 自动执行，本步可省略
- 中途停止：按 `Ctrl + C`

### 演练模式（不真实预约）

首次使用可先演练，流程与正式预约一致但不提交：在文件夹空白处 **Shift + 右键 → 在此处打开 PowerShell 窗口**，输入：

```
cmd /c "2-抢票.bat --test"
```

---

# 第二部分：查看与修改设置

## 2.1 查看当前可用场地

双击 `查看场次.bat`，显示当前配置地点的可约场次（已约满的不显示）：

```
  可预约 (6 个（仙林）):

  【16:00-17:00】
    1. 仙林体育馆主馆1F羽毛球1号场地
    2. 仙林体育馆主馆1F羽毛球2号场地
  【17:00-18:00】
    3. 仙林体育馆3F羽毛球馆1号场地
```

> 需要有效 token（先执行 1.4 第 1 步）；只显示**当前配置地点**的场地，想看别的校区先按下节修改地点。

## 2.2 配置向导：换校区 / 换场次

双击 `配置向导-选场地.bat`：

1. **学号确认**：直接回车保持不变（要改学号就输入新学号）
2. **地点菜单**：

   ```
   选择预约地点:
     1) 仙林     2) 三牌楼     3) 不限（全部）    0) 保持当前
   ```

   输入数字回车；`0` 或直接回车 = 不改
3. **选择场次**：屏幕显示带序号的场地表格，输入 **# 列序号**，多个用逗号分隔（`1,3,5`，中文逗号也行，重复自动忽略）。**顺序即优先级**，前面的抢不到自动顺延到后面
4. 选完自动保存并打印所选场次清单供核对

> **注意：在场次选择步骤直接按回车 = 清空全部预约目标**（不是"保持不变"）。只想改地点的话，也要把想要的场次序号重新输一遍。向导任意步骤按 `Ctrl + C` 可直接退出，配置不会被修改。

## 2.3 手动修改配置文件（可选）

部分设置（如每天开抢时间）不在向导里，可直接改文件：打开 `app\config\config.yaml`（右键 → 打开方式 → 记事本）：

```yaml
booking:
  location: "仙林"        # 预约地点："仙林" / "三牌楼" / ""（空=不限）
  targets:                # 要抢的场次，从上到下 = 优先级从高到低
    - court_name: "仙林体育馆主馆1F羽毛球1号场地"   # 场地名须与表格一字不差
      time: "16:00-17:00"                          # 时间段格式 "时:分-时:分"
  schedule_time: "12:00"  # 每天开抢时间，默认 12:00，一般不用改
```

保存后用 `查看场次.bat` 验证配置格式没改坏。一般情况**不需要手动改**，地点和场次用配置向导即可。

## 2.4 操作速查表

| 我想…… | 操作 |
|---|---|
| 首次安装配置 | 双击 `0-首次配置.bat` |
| 每天刷登录凭证 | 双击 `1-刷新token.bat` + 微信打开小程序场地页 |
| 抢票 | 双击 `2-抢票.bat`（12 点前） |
| 看今天有哪些场地 | 双击 `查看场次.bat` |
| 换校区 / 换场次 | 双击 `配置向导-选场地.bat` |
| 出问题自查 | 双击 `环境诊断.bat` |
| 改开抢时间（默认 12:00） | 改 `app\config\config.yaml` 的 `schedule_time` |
| 停止每天自动抢票 | 运行 `app\scripts\uninstall_task.bat` |
| 彻底卸载 | 双击 `卸载.bat`，再手动删文件夹 |

---

# 第三部分：计划任务

首配时输入 `y` 会注册两个 Windows 计划任务（仅当前用户，无需管理员）：

| 任务名 | 时间 | 动作 |
|---|---|---|
| BadmintonTokenRefresh | 每天 11:30 | 自动抓包刷新 token（届时需要在微信里打开一下小程序场地页） |
| BadmintonBooking | 每天 11:55 | 自动抢票（token 过期时会自动短时抓包兜底） |

**管理方式**：

- 查看：Win+R 输入 `taskschd.msc` 回车，在"任务计划程序库"中可见
- 卸载：运行 `app\scripts\uninstall_task.bat`
- 重新注册：以 PowerShell 运行 `app\scripts\install_task.ps1`

> 计划任务里的 11:30 刷新只是"打开抓包等待"，token 捕获仍需要微信端产生一次小程序访问；想完全无人值守，可保持微信在电脑上常开。

---

# 第四部分：常见问题速查

| 现象 | 原因和解决 |
|---|---|
| 双击 bat 弹"Windows 已保护你的电脑" | SmartScreen 提示 → **更多信息 → 仍要运行** |
| 首配时弹"安全警告"是否安装证书 | **点【是】**；误点"否"则重跑 `0-首次配置.bat` |
| 首配提示"CA 安装失败" | 弹出的安全警告超时未确认 → 重跑首配并及时点"是" |
| 小程序场地页显示"网络异常" | 抓包期间系统代理被临时切换，属正常；重试或重启电脑微信 |
| 一直捕获不到 token | ① 确认进的是场地预约页 ② 完全退出微信重开 ③ 关闭其他代理软件 ④ 跑环境诊断 |
| "端口 8080 被占用" | 关闭 Fiddler 等代理工具（脚本也会自动换临近端口） |
| 抢票提示 token 失效 | 重跑 `1-刷新token.bat` |
| 抢不到场次 | 12:00 竞争激烈属正常，脚本自动顺延 |
| 配置向导里回车后目标被清空 | 选场次那步"直接回车 = 清空" → 重跑向导把序号选回来 |
| 查看场次看不到别的校区 | 只显示当前配置地点 → 先配置向导换地点 |
| 想完全卸载 | 见第六部分 |

诊断利器：双击 `环境诊断.bat`，逐项检查 mitmdump / CA 证书文件 / CA 系统信任 / 端口 / 代理链路，五项全 `[OK]` 即环境正常。

---

# 第五部分：进阶——命令行用法

> 本部分适合想手动敲命令、或排查问题的用户。**绿色包已自带 Python，不需要安装任何环境**，只需用包内的 `python\python.exe`。

## 5.1 在绿色包目录打开命令行

1. 用文件资源管理器打开 `badminton-green` 文件夹
2. 点一下顶部地址栏，输入 `cmd`，回车
3. 黑色窗口打开，提示符为 `...\badminton-green>`，即可输入命令

常用按键：右键=粘贴；↑=上一条命令；Tab=补全文件名；Ctrl+C=终止运行；`cls`=清屏。

## 5.2 命令写法

绿色包的命令格式（在 `badminton-green` 目录下）：

```
python\python.exe app\脚本名.py [参数]
```

| 命令 | 作用 |
|---|---|
| `python\python.exe app\setup.py` | 首次配置向导（同 0-首次配置.bat） |
| `python\python.exe app\capture_token.py --wait 300` | 刷新 token（同 1-刷新token.bat） |
| `python\python.exe app\capture_token.py --refresh` | 强制刷新 token |
| `python\python.exe app\capture_token.py --check` | 环境诊断（同 环境诊断.bat） |
| `python\python.exe app\capture_token.py --install-cert` | 重装抓包证书 |
| `python\python.exe app\configure.py` | 配置向导（同 配置向导-选场地.bat） |
| `python\python.exe app\configure.py --slots` | 表格形式查看场地 |
| `python\python.exe app\book.py` | 抢票（同 2-抢票.bat） |
| `python\python.exe app\book.py --slots` | 查看场次（同 查看场次.bat） |
| `python\python.exe app\book.py --test` | 演练（不真实预约） |
| `python\python.exe app\book.py --now` | 立即抢（不等 12:00） |

## 5.3 源码版说明

如果使用的是本仓库源码（非绿色包），则需要自己准备环境：

1. 安装 [Python 3.10+](https://www.python.org/downloads/)（勾选 `Add python.exe to PATH`）
2. 安装 [mitmproxy](https://mitmproxy.org/downloads/)
3. 在项目目录双击 `setup.bat`（自动建虚拟环境、装依赖、引导配置）
4. 之后的命令把上表中的 `python\python.exe app\` 换成 `python ` 即可（如 `python book.py`）

源码版更多细节见仓库根目录的 `SOFTWARE.md` 与 `USAGE.md`。

---

# 第六部分：卸载与隐私

## 完全卸载

1. 双击绿色包根目录的 **`卸载.bat`**，自动完成四项清理：
   - 删除两个计划任务（BadmintonTokenRefresh / BadmintonBooking，如注册过）
   - 从当前用户证书存储删除 mitmproxy 根证书（含注册表，无残留）
   - 检查系统代理：仅当代理仍指向本机抓包端口（8080-8099）时恢复关闭——你自己的代理配置不会被改动
   - 删除 `~/.mitmproxy` 证书文件（会询问；本机另用 mitmproxy 软件可选 n 保留）
2. 手动删除 `badminton-green` 文件夹（脚本无法删除正在运行的自己）

清理完成后系统无任何残留：无注册表项、无后台服务、无驱动。
只想停止每天自动抢票而不卸载其他内容：运行 `app\scripts\uninstall_task.bat`。

## 隐私

- `app\config\config.yaml`（学号）和 `app\data\session_cache.json`（token）**只保存在你自己的电脑里，不要分享给他人**
- 脚本仅向 `wechat.njupt.edu.cn` 发送预约请求
- 抓包只读取南邮小程序流量，抓包结束后系统代理自动恢复原状
- 仅供学习交流使用，请遵守学校场馆管理规定，理性预约

---

# 附录：绿色包技术说明（给想了解原理的人）

- **运行环境**：`python\` 是 Python 3.10 嵌入式发行版（embeddable package），通过 `python310._pth` 配置 `sys.path` 指向包内 `Lib\site-packages` 和 `app\`，与系统 Python 完全隔离
- **抓包组件**：mitmproxy 11.0.2 以库形式内嵌（pip 自动选择兼容 3.10 的版本），`app\mitmproxy\bin\mitmdump.bat` 是调用 shim
- **与仓库源码的差异**（仅绿色包副本 `app\capture_token.py` 有三处补丁）：
  1. `find_mitmdump` 增加 shim 候选路径
  2. `spawn_mitmdump` 对 shim 改用内嵌 Python 直接启动（保证进程可干净终止）
  3. `install_ca_cert` 超时 15s → 120s（等待用户确认 Windows 证书对话框）
- **首配 bat 的额外步骤**：`0-首次配置.bat` 在运行向导前先用 mitmproxy 证书 API 预生成 CA（纯净机上 `~/.mitmproxy` 尚不存在，否则装证书会失败）
- **卸载**：绿色包新增 `卸载.bat` 与 `app\scripts\uninstall.py`（仓库版无此文件）
