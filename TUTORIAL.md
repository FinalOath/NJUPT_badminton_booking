# 南邮羽毛球预约脚本使用教程

> 本教程介绍如何在 Windows 自带的命令提示符（CMD）中使用本脚本：如何打开命令行、输入命令、查看输出信息，以及完整的安装与每日抢票流程。
> 全程无需编写任何代码，所有命令均为现成命令，直接输入或复制粘贴后回车即可。

---

## 第 0 课：基本概念

| 名词 | 说明 |
|---|---|
| **CMD（命令提示符）** | Windows 自带的命令行窗口（黑色窗口）。在其中输入命令并回车，系统执行后将结果显示在窗口中。本教程中的所有命令都在此窗口中输入。 |
| **项目文件夹** | 项目压缩包解压后得到的文件夹，里面有 `setup.bat`、`book.py` 等文件。后续所有命令都需要在该文件夹下运行。 |

**使用方式**：本脚本没有图形界面，所有操作 = 打开 CMD → 输入一条命令 → 按回车 → 查看结果。

---

## 第 1 课：怎么打开 CMD（黑色窗口）

有三种方法，**推荐方法 2**，可直接在项目文件夹中打开，省去切换目录的步骤。

### 方法 1：Win + R（通用，但要多走一步）

1. 同时按键盘上的 **Windows 键 + R**，屏幕左下角弹出"运行"小窗口
2. 输入三个字母 `cmd`，按回车
3. 黑色窗口打开，第一行大概长这样：

   ```
   C:\Users\你的名字>
   ```

   这表示当前目录是 `C:\Users\你的名字`，并非项目文件夹，还需按第 2 课的方法切换到项目目录。

### 方法 2：在项目文件夹里直接打开（推荐）

1. 用**文件资源管理器**（就是平时看文件的黄色文件夹图标）打开你的项目文件夹，例如 `D:\badminton`
2. 用鼠标**点一下窗口顶部的地址栏**（显示 `D:\badminton` 的那一条）
3. 把里面的路径删掉，输入 `cmd`，按回车
4. 黑色窗口打开，光标前面直接就是：

   ```
   D:\badminton>
   ```

   说明当前目录已是项目文件夹，可跳过第 2 课，直接输入命令。

### 方法 3：右键菜单（Win11）

在文件夹空白处**按住 Shift 点右键** → 选"在此处打开 PowerShell 窗口"或"在终端中打开"。PowerShell 是 CMD 的"亲戚"，本教程的命令在它里面也能用。

### 认识这个黑色窗口

```
D:\badminton>_
```

- `D:\badminton` —— 当前目录。**输入命令前请先确认此处显示的是项目文件夹，否则会报"找不到文件"。**
- `>` —— 分隔符，其后为输入位置
- 闪动的 `_`（光标）—— 等待输入

---

## 第 2 课：在 CMD 中输入命令

输入命令的基本流程：**输入命令 → 按回车 → 查看结果**。规则如下：

### 基本规则

1. **命令输完后按回车才会执行**。没按回车之前可以用退格键修改。
2. **字母、横线、空格需输入准确**。例如 `python book.py --slots` 里：
   - `python` 和 `book.py` 之间有**一个空格**
   - `--slots` 前面也有**一个空格**，开头是**两个短横线**（不是一长横）
3. **大小写一般不敏感**，`Python` 和 `python` 均可，建议统一使用小写。
4. 命令中空格后面的部分称为"参数"，用于指定本次执行的方式：
   - `python book.py` = 正常抢票
   - `python book.py --test` = 只是演练，不真的预约
   - `python book.py --now` = 不等 12 点，现在就抢

### 常用按键技巧

| 你想干什么 | 怎么按 |
|---|---|
| **粘贴**（复制来的命令） | 在窗口里**点鼠标右键**（大部分版本直接粘贴）；不行就试 `Ctrl + V` |
| **复制**窗口里的文字 | 用鼠标左键拖选文字，然后点**右键**（或按回车） |
| **重复上一条命令** | 按 **↑ 上箭头**，按一下翻一条历史命令 |
| **自动补全文件名** | 打文件名前几个字母，按 **Tab** 键 |
| **紧急停止**正在运行的命令 | 按 **Ctrl + C**（抓包不想等了就用这个） |
| **清空屏幕** | 输入 `cls` 回车 |

### 切换目录：cd 命令

`cd` 用于进入指定文件夹。假设项目位于 `D:\badminton`：

**情况 A：项目在 D 盘，而窗口显示 C 盘** → 要分两步：

```
D:
```
（输入盘符加冒号回车，先切换到 D 盘）

```
cd \badminton
```
（回车 —— 再进入 badminton 文件夹）

**情况 B：项目就在当前盘的某个文件夹** → 一条命令：

```
cd D:\badminton
```

**快捷技巧**：在文件资源管理器中将项目文件夹**直接拖入 CMD 窗口**，路径会自动填入光标处。即输入 `cd `（cd + 空格）→ 拖入文件夹 → 回车即可。

> 使用第 1 课的"方法 2"在项目文件夹中打开 CMD，则无需执行 `cd`。

---

## 第 3 课：第一次安装（只做一次）

### 第 1 步：确认 Python 装好了

在 CMD 里输入：

```
python --version
```

- **显示** `Python 3.10.x`（或更高）→ 已安装，跳到第 2 步
- **显示** `'python' 不是内部或外部命令，也不是可运行的程序` → Python 没装好：
  1. 打开 https://www.python.org/downloads/ 下载 **Windows installer (64-bit)**
  2. 安装时**第一屏最下方一定勾选 `Add python.exe to PATH`**（漏勾就会出现上面这个错误，只能重装）
  3. 装完**关掉 CMD 重新打开**，再输 `python --version` 验证

### 第 2 步：检查项目放的位置对不对

项目解压后的路径**不能有中文、不能有空格**：

- 好的例子：`D:\badminton`、`D:\njupt\booking`
- 坏的例子：`D:\下载\羽毛球预约`、`C:\Users\张三\Desktop\badminton booking`

路径不符合要求时，请关闭 CMD，将文件夹移至合适位置后重新打开 CMD。

### 第 3 步：运行安装向导

在项目文件夹里**双击 `setup.bat`**（也可以在 CMD 里输入 `setup.bat` 回车）。

它会自动做 3 件事，屏幕上有 `[1/3]` `[2/3]` `[3/3]` 的进度提示：

1. 建一个"虚拟环境"（在项目里生成 `.venv` 文件夹，不影响系统）
2. 联网下载安装依赖（几个 Python 库，需要网）
3. 问你两个问题：
   - **学号**：直接打字输入你的学号，回车
   - **是否注册每日自动任务**：输入 `y` 回车（推荐，注册后每天 11:55 电脑自动抢票）

看到 `=== 完成 ===` 即安装成功。**此步骤仅需执行一次。**

> **双击 `setup.bat` 闪退看不清报错？** 改用 CMD 手动运行：在 CMD（已 cd 到项目文件夹）里输入 `setup.bat` 回车，报错会停在屏幕上让你看清原因。

---

## 第 4 课：每日使用流程（两条命令）

### 命令 1：刷新登录凭证（每天抢票前做一次）

```
python capture_token.py --wait 300
```

> 名词解释：**token** 为微信登录状态的临时凭证，约 3 小时过期，因此每天抢票前需刷新一次。`--wait 300` 表示等待 300 秒供用户操作微信。

输入回车后，按顺序操作：

1. 屏幕显示"等待新 token"等信息，**请勿关闭该窗口**
2. 打开**电脑微信** → 找到**南邮小程序** → 点进**场地预约页面**
3. 等待 5~20 秒，屏幕出现 `[+] 新 token 已捕获` 即成功，脚本自动退出
4. 若一直无法捕获，请将**电脑微信完全退出**（右下角托盘图标右键 → 退出）后重新打开，再次进入小程序场地页

### 命令 2：抢票

```
python book.py
```

- **12 点前运行**：脚本将自动等待至 12:00:00 准时发起预约
- 预约成功会显示成功信息；首选场次已满时**自动顺延**至其他场次
- 如果安装时注册了计划任务，**每天 11:55 自动抢票**，这条命令只是手动备用
- 想中途停止：按 `Ctrl + C`

### 演练模式（不真实预约）

首次使用可先运行演练模式，流程与正式预约一致但不实际提交：

```
python book.py --test
```

---

## 第 5 课：怎么查看各种信息

所有查看操作的方式相同：**在 CMD 中输入对应命令 → 回车 → 查看输出**。

| 你想看什么 | 输入这条命令 | 会看到什么 |
|---|---|---|
| Python 装没装好 | `python --version` | 版本号，如 `Python 3.12.3` |
| 环境是否全部正常 | `python capture_token.py --check` | 逐项检查，每项后面跟 `[OK]` 或问题提示 |
| 今天有哪些场地/场次 | `python configure.py --slots` | 一张表格，列出场地名和时间段 |
| 同上（简版） | `python book.py --slots` | 场次列表（只读，不改配置） |
| 本机抢票速度 | `python benchmark.py` | 首枪延迟等性能数字 |
| 你的学号/选的场地 | 用记事本打开 `config\config.yaml` | 配置文件内容 |

### 输出标志含义

脚本输出的每行带有前缀标志，含义如下：

| 标志 | 意思 | 处理方式 |
|---|---|---|
| `[+]` | 成功完成一项操作 | 无需处理 |
| `[OK]` | 检查通过 | 无需处理 |
| `[*]` | 进行中/提示信息 | 了解即可 |
| `[!]` | 警告 | 注意查看，可能需要操作 |
| `[FAIL]` / `[ERROR]` | 失败 | **查看其后说明的原因**，并参考第 7 课解决 |

---

## 第 6 课：常用操作专题（查场地 / 改地点 / 改时间段）

这一课介绍三个最常用的操作：**查询场地、修改地点、修改场次**。
所有操作的前提相同：CMD 已打开，且当前目录为项目文件夹。

### 6.1 查看当前可用场地（去哪儿查，怎么查）

**查询位置**：CMD 中，无需打开网站或小程序。

**前提**：查询需要有效的登录凭证（token）。如屏幕提示 `[-] 无有效 token`，请先按第 4 课执行：

```
python capture_token.py --wait 300
```

捕获 token 后再执行查询。

**方法一：表格版（最直观，推荐）**

```
python configure.py --slots
```

屏幕上出现按时间段分组的表格，长这样：

```
┏━━━┳━━━━━━━━━━━━━━━━━━━━━━━━━━┳━━━━━━━━━━━━━┓
┃ # ┃ 场地名                    ┃ 时间段       ┃
┡━━━╇━━━━━━━━━━━━━━━━━━━━━━━━━━╇━━━━━━━━━━━━━┩
│ 1 │ 仙林体育馆主馆1F羽毛球1号 │ 16:00-17:00 │
│ 2 │ 仙林体育馆主馆1F羽毛球2号 │ 16:00-17:00 │
└───┴──────────────────────────┴─────────────┘
```

**方法二：纯文字列表（更快）**

```
python book.py --slots
```

屏幕上出现：

```
  可预约 (6 个（仙林）):

  【16:00-17:00】
    1. 仙林体育馆主馆1F羽毛球1号场地
    2. 仙林体育馆主馆1F羽毛球2号场地
  【17:00-18:00】
    3. 仙林体育馆3F羽毛球馆1号场地
```

**结果说明**：

1. 仅显示**当前可预约**的场次，已约满的不显示。
2. 仅显示**当前配置地点**（默认仙林）的场地。如需查看三牌楼，请先按 6.2 修改地点后再查询。

### 6.2 调整预约地点（仙林 / 三牌楼）

在 CMD 里输入：

```
python configure.py
```

然后按屏幕提示依次操作：

1. **学号确认**：直接回车 = 保持不变（如需修改学号，输入新学号后回车）
2. **然后出现地点菜单**：

   ```
   选择预约地点:
     1) 仙林     2) 三牌楼     3) 不限（全部）    0) 保持当前
   请选择 (默认 仙林):
   ```

   输入数字 `1`、`2` 或 `3` 后回车；输入 `0` 或直接回车 = 不修改地点
3. 随后显示该地点的场地表格，进入场次选择环节（见 6.3）

> **中途退出**：在向导的任意步骤按 `Ctrl + C` 可直接退出，配置不会被修改。

### 6.3 调整时间段（选择目标场次）

接着 6.2 的向导，选完地点后屏幕会显示带**序号**的场地表格，然后提示：

```
选择要预约的场次（按优先级，从高到低）:
  例：输入 1,3,5 表示 1、3、5 号场次，先抢 1 号
序号（逗号分隔，回车=清空目标）:
```

操作规则：

- 输入表格中 **# 列的序号**，多个以逗号分隔，例如 `2,4,6`
- **顺序即优先级**：靠前的优先抢，失败后自动顺延至下一个
- 支持中文逗号 `，`；重复的序号自动忽略
- 确认回车后，脚本自动保存并输出所选场次清单供核对

> **注意：在场次选择步骤直接按回车 = 清空全部预约目标**（而非"保持不变"）。
> 如仅需修改地点、保留原场次，也需重新输入所需场次序号；
> 如不慎清空，重新运行 `python configure.py` 选择即可。

> 预约日期固定为**当天**（每天中午 12:00 抢当天的场），不需要也不能改日期。

### 6.4 手动修改配置文件（可选，用于修改开抢时间等）

部分设置（如每天的开抢时间）不包含在配置向导中，可直接修改配置文件：

1. 文件资源管理器进入项目文件夹 → 打开 `config` 文件夹 → 右键 **`config.yaml`** → "打开方式" → **记事本**
2. 找到 `booking:` 这一段，关键字段含义：

   ```yaml
   booking:
     location: "仙林"        # 预约地点："仙林" / "三牌楼" / ""（空=不限）
     targets:                # 要抢的场次列表，从上到下 = 优先级从高到低
       - court_name: "仙林体育馆主馆1F羽毛球1号场地"   # 场地名必须和表格里一字不差
         time: "16:00-17:00"                          # 时间段格式 "时:分-时:分"
     schedule_time: "12:00"  # 每天开抢时间，默认 12:00，一般不用改
   ```

3. 修改后按 `Ctrl + S` 保存并关闭
4. **验证**：在 CMD 中运行 `python book.py --slots`，能正常列出场次即说明格式正确

> 一般情况下**无需手动修改此文件**——地点和场次通过 `python configure.py` 设置即可，仅修改 `schedule_time` 等高级设置时需要本节操作。

### 6.5 常用操作速查表

| 我想…… | 在哪儿操作 | 具体怎么做 |
|---|---|---|
| 看今天有哪些场地可约 | CMD | `python configure.py --slots`（表格）或 `python book.py --slots`（列表） |
| 换预约地点 | CMD | `python configure.py` → 地点菜单输 `1`/`2`/`3` |
| 换要抢的场次/时间段 | CMD | `python configure.py` → 表格里按序号选，如 `1,3,5` |
| 改学号 | CMD | `python configure.py` → 第一步提示学号时输入新学号 |
| 改每天开抢时间 | 记事本 | 改 `config\config.yaml` 里的 `schedule_time` |
| 改完后确认没改错 | CMD | `python book.py --slots` 复查 |

---

## 第 7 课：出问题怎么办（常见问题速查）

| 屏幕上看到 | 原因和解决 |
|---|---|
| `'python' 不是内部或外部命令` | Python 没装或装时没勾 `Add python.exe to PATH` → 重装并勾选 |
| `系统找不到指定的路径` / `No such file` | 你**没站在项目文件夹里** → 看光标前的路径，用 `cd` 走过去（第 2 课） |
| 双击 `setup.bat` 一闪而过 | 在 CMD 里手动输入 `setup.bat` 回车，看清报错 |
| `--check` 显示 **CA 未被信任** | 运行 `python capture_token.py --install-cert`，然后**重启电脑微信** |
| 打开小程序显示"网络异常" | 抓包期间系统代理被临时切换，属正常；重试，仍不行就**重启电脑微信** |
| **端口 8080 被占用** | 开着 Fiddler 等其他抓包软件 → 关掉它们 |
| 抢票提示 **token 失效** | 运行 `python capture_token.py --refresh` 刷新后重试 |
| 抢不到场次 | 12:00 竞争激烈属正常，脚本会自动顺延到其他场次 |
| 配置向导里回车后发现目标被清空 | 选场次那一步"直接回车 = 清空" → 重跑 `python configure.py` 把序号选回来 |
| `--slots` 看不到想看的校区 | 查场地只显示当前配置的地点 → 先 `python configure.py` 换地点再查 |

如果以上方法均无法解决：将窗口中的报错信息**用鼠标拖选 → 右键复制**，反馈给项目维护者或协助排查的人员。

---

## 附录：命令总表

> 使用前提：CMD 已打开，且光标前显示的是你的项目文件夹。

| 命令 | 作用 | 什么时候用 |
|---|---|---|
| `python --version` | 看 Python 版本 | 装完 Python 验证 |
| `setup.bat` | 一键安装 | 只装一次 |
| `python capture_token.py --check` | 环境诊断 | 出问题先跑它 |
| `python capture_token.py --wait 300` | 刷新 token | **每天抢票前** |
| `python capture_token.py --refresh` | 强制刷新 token | 提示 token 失效时 |
| `python capture_token.py --install-cert` | 重装证书 | 提示 CA 未信任时 |
| `python configure.py` | 选地点/场次向导 | 想改场地时 |
| `python configure.py --slots` | 只看场地表格 | 随时 |
| `python book.py --slots` | 查看场次 | 随时 |
| `python book.py --test` | 演练（不实际预约） | 首次使用测试 |
| `python book.py` | 抢票（自动等到 12:00） | 手动抢 |
| `python book.py --now` | 立即抢（不等 12:00） | 应急 |
| `python benchmark.py` | 测抢票速度 | 需要时 |

**每日完整流程：**

```
python capture_token.py --wait 300     ← 回车后去电脑微信打开南邮小程序场地页
python book.py --test                  ← 首次使用可先演练（可选）
python book.py                         ← 12 点前运行
```

---

## 隐私与安全提醒

- `config\config.yaml`（含你的学号）和 `data\session_cache.json`（含登录 token）**只保存在你自己的电脑里，不要发给任何人**
- 脚本只向 `wechat.njupt.edu.cn` 发送预约请求
- 仅供学习交流使用，请遵守学校场馆管理规定，理性预约
