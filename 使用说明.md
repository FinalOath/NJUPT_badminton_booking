# 🏸 南邮羽毛球预约脚本 - 使用说明

自动化预约南邮小程序中的羽毛球场。每天 12:00 开放抢购，本脚本自动在 12:00:00 精确抢购并自动刷新登录 token。

> ⚠️ **仅供学习交流使用**，请遵守学校管理规定，理性使用预约资源。

## 两种安装方式（任选其一）

### 方式 A：自装版（推荐给愿意自己动手的同学）
1. 先按《软件下载清单.md》装好 **Python** 和 **mitmproxy**
2. 下载本项目 zip，解压到任意目录（路径不含中文和空格）
3. 双击 **`setup.bat`** → 自动装依赖、填学号、装证书、注册计划任务

### 方式 B：打包版（直接下载完整包）
1. 下载已内置 Python 运行环境 + mitmproxy 的完整包
2. 解压后双击 **`setup.bat`** 即可

> 两种方式装完后用法完全一样。

## 安装完成后（首次使用）

1. 编辑 `config\config.yaml`，确认：
   - `auth.student_id`: 你的学号
   - `booking.targets`: 想抢的场次（可增删）
2. 运行一次抓包验证：
   ```bash
   python capture_token.py --check
   ```
   应显示全部 `[OK]`。

## 每日使用

```bash
# 刷新 token（起代理 → 在电脑微信打开南邮小程序 → 进入场地页 → 自动捕获）
python capture_token.py --wait 300

# 配置向导：用表格选场地/地点（仙林/三牌楼）/时间段，自动写回配置
python configure.py

# 查看某天可约场次（只读，不抢）
python configure.py --slots    # 场地表格（推荐，含 id-场地对照）
python book.py --slots

# 演练模式（不实际预约，验证配置）
python book.py --test

# 正式抢票（自动等到 12:00）
python book.py
```

如果注册了计划任务（`setup.bat` 里可选），则每天 **11:30 自动刷新 token**、**11:55 自动抢票**，你只需在 **11:30 前后打开一次电脑微信里的南邮小程序**（约 10 秒），其余全自动。

## 命令速查

| 命令 | 作用 |
|---|---|
| `python capture_token.py --check` | 环境诊断 |
| `python capture_token.py --refresh` | 强制刷新 token |
| `python capture_token.py --install-cert` | 重装证书（误删时） |
| `python book.py --slots` | 查看场次 |
| `python book.py --test` | 演练 |
| `python book.py --now` | 立即抢（不等 12:00） |
| `python book.py --date 2026-06-10` | 抢指定日期 |

## 常见问题

| 现象 | 处理 |
|---|---|
| `--check` 显示 "CA 未被信任" | 运行 `python capture_token.py --install-cert`，然后**重启电脑微信** |
| 打开小程序"网络异常" | 抓包期间系统代理临时切换，**重启电脑微信**再开小程序 |
| `--check` 显示 "端口 8080 被占用" | 关闭 Fiddler 等其他代理工具 |
| 抢票时提示 token 失效 | 运行 `python capture_token.py --refresh` 后重试 |
| 已有 token 但显示"过期" | token 有效期仅约 3 小时，11:30 刷新或提前手动刷新 |

## 隐私说明

- `config\config.yaml` 含你的学号；`data\session_cache.json` 含你的登录 token —— 都只在本机，**不要分享给别人**
- 脚本只在预约时向 `wechat.njupt.edu.cn` 发送请求
